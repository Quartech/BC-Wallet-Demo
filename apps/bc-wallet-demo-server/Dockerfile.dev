# Development Dockerfile for bc-wallet-demo-server with hot reloading
FROM node:20-bookworm

# Install pnpm globally
RUN npm install -g pnpm

# Set working directory
WORKDIR /app

# Copy package files for dependency installation
COPY package*.json pnpm-lock.yaml pnpm-workspace.yaml turbo.json tsconfig.json tsconfig.build.json tsconfig.eslint.json ./

# Copy packages directory (needed for workspace dependencies)
COPY packages/ ./packages/

# Copy the demo-server app
COPY apps/bc-wallet-demo-server/ ./apps/bc-wallet-demo-server/

# Install dependencies
RUN pnpm install

# Set working directory to the demo-server app
WORKDIR /app/apps/bc-wallet-demo-server

# Expose the development server port
EXPOSE 5000

# Set environment variables for development
ENV NODE_ENV=development
ENV PORT=5000

# Start the development server with hot reloading using ts-node-dev
CMD ["pnpm", "dev"]
